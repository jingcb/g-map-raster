#ifndef __RULE__H__
#define __RULE__H__
namespace gmap {
    // the rule to render map ! 
    class Rule {
    public:
        Rule();
        ~Rule();
        
        bool HasMember(std::string);
        
    };
}
#endif
